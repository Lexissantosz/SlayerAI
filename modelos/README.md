# Modelos

O modelo portatil esperado pela v0.1 e:

```text
player_v01.onnx
```

Ele e gerado localmente por:

```powershell
python src/exportar_onnx.py
```

Depois de validado, o arquivo ONNX pode ser versionado no repositorio.
Pesos intermediarios `.pt`, arquivos `.engine` e resultados de treino
continuam locais.
